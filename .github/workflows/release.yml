name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-macos-arm64:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: swift build -c release
      - name: Package
        run: |
          mkdir -p dist
          cp .build/release/orrery-sync dist/orrery-sync
          chmod +x dist/orrery-sync
          tar -czf orrery-sync-darwin-arm64.tar.gz -C dist orrery-sync
      - uses: actions/upload-artifact@v4
        with:
          name: orrery-sync-darwin-arm64
          path: orrery-sync-darwin-arm64.tar.gz

  build-linux-x86_64:
    runs-on: ubuntu-latest
    container: swift:6.0-jammy
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: swift build -c release --static-swift-stdlib
      - name: Package tar.gz
        run: |
          mkdir -p dist
          cp .build/release/orrery-sync dist/orrery-sync
          chmod +x dist/orrery-sync
          tar -czf orrery-sync-linux-x86_64.tar.gz -C dist orrery-sync
      - name: Package deb
        run: |
          tag="${GITHUB_REF#refs/tags/v}"
          mkdir -p deb/DEBIAN deb/usr/local/bin
          cp .build/release/orrery-sync deb/usr/local/bin/orrery-sync
          chmod +x deb/usr/local/bin/orrery-sync
          cat > deb/DEBIAN/control <<CTRL
          Package: orrery-sync
          Version: ${tag}
          Architecture: amd64
          Maintainer: OffskyLab <offskylab@users.noreply.github.com>
          Description: P2P real-time sync daemon for Orrery
          CTRL
          sed -i 's/^          //' deb/DEBIAN/control
          dpkg-deb --build deb orrery-sync_${tag}_amd64.deb
      - uses: actions/upload-artifact@v4
        with:
          name: orrery-sync-linux-x86_64
          path: |
            orrery-sync-linux-x86_64.tar.gz
            orrery-sync_*_amd64.deb

  build-linux-arm64:
    runs-on: ubuntu-24.04-arm
    container: swift:6.0-jammy
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: swift build -c release --static-swift-stdlib
      - name: Package tar.gz
        run: |
          mkdir -p dist
          cp .build/release/orrery-sync dist/orrery-sync
          chmod +x dist/orrery-sync
          tar -czf orrery-sync-linux-arm64.tar.gz -C dist orrery-sync
      - name: Package deb
        run: |
          tag="${GITHUB_REF#refs/tags/v}"
          mkdir -p deb/DEBIAN deb/usr/local/bin
          cp .build/release/orrery-sync deb/usr/local/bin/orrery-sync
          chmod +x deb/usr/local/bin/orrery-sync
          cat > deb/DEBIAN/control <<CTRL
          Package: orrery-sync
          Version: ${tag}
          Architecture: arm64
          Maintainer: OffskyLab <offskylab@users.noreply.github.com>
          Description: P2P real-time sync daemon for Orrery
          CTRL
          sed -i 's/^          //' deb/DEBIAN/control
          dpkg-deb --build deb orrery-sync_${tag}_arm64.deb
      - uses: actions/upload-artifact@v4
        with:
          name: orrery-sync-linux-arm64
          path: |
            orrery-sync-linux-arm64.tar.gz
            orrery-sync_*_arm64.deb

  release:
    needs: [build-macos-arm64, build-linux-x86_64, build-linux-arm64]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          tag="${GITHUB_REF#refs/tags/}"
          gh release create "$tag" \
            --repo "$GITHUB_REPOSITORY" \
            --title "$tag" \
            --generate-notes \
            artifacts/orrery-sync-darwin-arm64/orrery-sync-darwin-arm64.tar.gz \
            artifacts/orrery-sync-linux-x86_64/orrery-sync-linux-x86_64.tar.gz \
            artifacts/orrery-sync-linux-x86_64/orrery-sync_*_amd64.deb \
            artifacts/orrery-sync-linux-arm64/orrery-sync-linux-arm64.tar.gz \
            artifacts/orrery-sync-linux-arm64/orrery-sync_*_arm64.deb

  update-apt:
    needs: [release]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Checkout APT repo
        uses: actions/checkout@v4
        with:
          repository: OffskyLab/apt
          token: ${{ secrets.APT_REPO_TOKEN }}
          path: apt-repo

      - name: Update APT repo
        run: |
          mkdir -p apt-repo/pool/main/o/orrery-sync
          cp artifacts/orrery-sync-linux-x86_64/orrery-sync_*_amd64.deb apt-repo/pool/main/o/orrery-sync/
          cp artifacts/orrery-sync-linux-arm64/orrery-sync_*_arm64.deb apt-repo/pool/main/o/orrery-sync/

          cd apt-repo
          dpkg-scanpackages --arch amd64 pool/ > dists/stable/main/binary-amd64/Packages
          dpkg-scanpackages --arch arm64 pool/ > dists/stable/main/binary-arm64/Packages
          mkdir -p dists/stable/main/binary-i386
          touch dists/stable/main/binary-i386/Packages
          gzip -kf dists/stable/main/binary-amd64/Packages
          gzip -kf dists/stable/main/binary-arm64/Packages
          gzip -kf dists/stable/main/binary-i386/Packages

          cd dists/stable
          cat > Release <<RELEASE
          Origin: OffskyLab
          Label: Orrery
          Suite: stable
          Codename: stable
          Architectures: amd64 arm64 i386
          Components: main
          Description: APT repository for Orrery
          RELEASE
          sed -i 's/^          //' Release
          apt-ftparchive release . >> Release

      - name: Push APT repo
        run: |
          cd apt-repo
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet || git commit -m "[UPDATE] orrery-sync $(echo ${GITHUB_REF#refs/tags/})"
          git push
